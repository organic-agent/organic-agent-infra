{
	admin off
	auto_https disable_redirects
}

https://${fqdn}:${proxy_https_port} {
	bind 127.0.0.1

	tls {
		dns route53 {
			hosted_zone_id ${zone_id}
		}
	}

	reverse_proxy 127.0.0.1:${app_port}
}
